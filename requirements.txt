# Whole Life Journey - Dependencies
# Django Core
Django>=5.1,<6.0
django-environ>=0.11.2

# Database
psycopg2-binary>=2.9.9

# Authentication
django-allauth>=65.0.0
django-axes>=6.0.0  # Rate limiting for authentication (Security Fix H-3)

# Forms and Crispy
django-crispy-forms>=2.3
crispy-tailwind>=1.0.3

# Static Files and Media
whitenoise>=6.7.0
pillow>=10.4.0
cloudinary>=1.40.0
django-cloudinary-storage>=0.3.0

# AWS S3 (for Capture audio files)
boto3>=1.34.0

# HTMX Integration
django-htmx>=1.19.0

# Markdown Support (for help system)
markdown>=3.7

# Time Zone Support
pytz>=2024.1

# Date utilities
python-dateutil>=2.8.2

# Google Calendar Integration
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# Production Server
gunicorn>=23.0.0

# Development Tools (optional, remove in production if needed)
django-debug-toolbar>=4.4.6
django-extensions>=3.2.3
django-watchfiles>=1.4.0  # Efficient autoreload (fixes Python 3.14 StatReloader issue)

# Email (Resend)
resend>=2.4.0

# AI Integration
openai>=1.0.0

# SMS Notifications (Twilio)
twilio>=9.0.0

# Bank Integration (Plaid)
plaid-python>=23.0.0
cryptography>=42.0.0

# Background Job Scheduler
django-apscheduler>=0.6.2

# Linting and Code Quality (development)
black>=24.8.0
isort>=5.13.2
flake8>=7.1.1

# Error Tracking (Sentry)
sentry-sdk>=2.0.0

# Test Coverage
coverage>=7.4.0

# Document Generation
python-docx>=1.1.0  # Word document generation (no system dependencies needed)

# Medical Lab PDF Ingestion
pdfplumber>=0.11.0  # PDF text extraction for lab results

# Push Notifications (APNs)
apns2>=0.7.2

# Payment Processing (Stripe)
stripe>=7.0.0
dj-stripe>=2.8.0

# Force rebuild: 2026-01-14-v3 (Clean nixpacks build, no WeasyPrint)
