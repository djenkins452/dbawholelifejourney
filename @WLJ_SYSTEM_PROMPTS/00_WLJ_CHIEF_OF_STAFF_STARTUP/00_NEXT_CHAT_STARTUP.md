# 00 · NEXT CHAT STARTUP  (read me first — the bootloader)

**You are starting a WLJ Chief of Staff session. This is the only file to read before you begin.** It is the **one temporary document** in this package; everything else here is permanent institutional memory.

## Do this now
1. **Read the rest of this package, in order:** `01_READ_FIRST…ARCHITECTURE` → `02_WLJ_CONSTITUTION` → `03_ENGINEERING_OPERATING_GUIDE` → `04_DANNY_WORKING_PREFERENCES` → `98_SESSION_TRANSITION_PROTOCOL` → `99_REFERENCE_INDEX`.
2. **Those documents are the authoritative source of truth.** Every permanent decision, principle, rule, and preference is already folded into them. **Do not summarize them back.** Read, absorb, act.
3. **Do not revisit constitutional decisions** unless a change genuinely requires a **Constitutional Review** (`02 §3`, default NO, Danny's explicit written approval).
4. Continue from the live sprint state below.

---

> ## 💸 COST GOVERNANCE (2026-08-16) — READ BEFORE ANY MODEL TESTING
> **Incident:** Danny's OpenAI account auto-charged ~$16–17 repeatedly. Root cause = **Claude's own
> certification / `cos-run` real-model testing** (Aug 12–16) × the **Executive Synthesis Phase-2
> multiplier** (a broad turn = 3–9 billable requests). Forensic record: **`docs/WLJ_OPENAI_COST_AUDIT.md`**.
> **GOVERNANCE MILESTONE SHIPPED (§16 of that doc):** every billable provider request now writes ONE
> `owner_finance.LLMUsageEvent` via the single seam `apps/ai/llm_accounting.py :: record_llm_event`
> (tokens+cost+`source`+`traffic_class`); certification traffic is tagged `certification`, proactive as
> `proactive`; **generate-before-suppress waste eliminated** (midday/evening now gate + lock before the
> model call). See spend at `/owner/finance/` or `GET /admin-console/api/claude/cost-summary/?days=7`.
> **Testing discipline is now MANDATORY (CLAUDE.md + `03 §10a`): Tier 1 deterministic by default; ONE
> real-model smoke per deploy; never default to repeated real-model runs — answer the 4 questions first.**
> Model (`gpt-4o`), Executive Synthesis, schedules, retries UNCHANGED (cost is now visible, not optimized).
> **✅ Proactive Product Phase 2 COMPLETE (2026-08-17):** M2 Durable Follow-Through (`ConversationFollowUp`
> + native `schedule_follow_up` tool, fired by PGS, authored fresh by the certified CoS), M3 Missing-Data
> (`get_data_health` on-demand truth over `health_sync_status`), M4 Action Completeness (exposed the DAY1-safe
> high-leverage actions). All prod-deployed + validated; integrated day-long cert passed. Cost telemetry
> preserved (proactive turns ledgered `traffic_class=proactive`). Detail: `WLJ_COS_PROACTIVE_PRODUCT_INVESTIGATION.md`
> §22. Residuals minor (follow-up firing time-delayed; `resolve_pending_action` audit-linkage; read-after-write
> SAE snapshot staleness — all pre-existing/minor). **Awaiting Danny's app-level validation + cleanup of cert
> test artifacts** (a few events/tasks/follow-ups on his account from real-model smokes).

*Regenerated at the end of every chat by `@WLJ_SYSTEM_PROMPTS/99_PREPARE_NEXT_CHAT.md`. Live sprint state only — nothing constitutional, architectural, or duplicated.*

**Last regenerated:** 2026-08-14 (**🧠 CHIEF OF STAFF EXECUTIVE-TRUTH ARC — IMPLEMENTED + PRODUCTION-CERTIFIED; ONE residual OPEN (the FIRST active CoS item — see the ACTIVE CoS ARCHITECTURE TRACK section).** This CoS-architecture session (a separate track from the Health Question Certification primary focus below, which is PRESERVED intact for its owning session) executed the deferred "Executive Truth grounding" milestone end-to-end against the original governing model — *WLJ owns deterministic truth; OpenAI owns interpretation/judgment; OpenAI decides what it needs; WLJ supplies it; OpenAI decides when it has enough; OpenAI thinks; Clara acts as Chief of Staff.* Shipped + certified across a long MASTER-PROMPT arc: **model-directed broad truth retrieval · broad-answer continuity · removed `current_action` over-steer / WLJ-side intent steering · model-directed retrieval persistence · Danny-specific grounding across framings · personalized "show-your-work" retrieval · investigation-sufficiency + broad-scope · the current-judgment-needs-current-evidence rule (standing ORIENTS ≠ evidence; never a 0-tool current judgment from `deterministic_understanding`) · durable CoS turn lifecycle · the bounded two-phase Executive Synthesis Phase · evidence-payload slimming · narrow-query regression protection · Phase-2 latency correction.** Durable knowledge folded UP: architecture → `01 §5`/`§6` (Executive Synthesis two-phase lesson + maturity), engineering → `03 §11` (the temp-infra `git add`-abort outage lesson), index → `99 §B` (`WLJ_COS_EXECUTIVE_SYNTHESIS_PHASE.md`). **PRODUCTION INCIDENT LESSON (now permanent, `03 §11`): a temp-experiment cleanup caused a ~40-min full-site 502 because a `git add` that included an already-`git rm`'d path fatal-aborted the whole add → only the deletion committed while the `urls.py`/`AppConfig.ready()` edits stayed uncommitted → orphaned `__import__` of the deleted module crashed web (URLconf) + worker/beat (`ready()`); local `check` passed against the uncommitted fix. Removed, recovered, verified absent.** The **FIRST ACTIVE CoS ITEM** for next session is the newly-exposed heuristic-leakage / evidence-lineage residual — see its section below. All parallel tracks, Meal Intelligence, deferred roadmap, and Waiting-on-Danny are PRESERVED exactly.**)

<!-- PRIOR REGEN (2026-07-30): 🩺 HEALTH QUESTION CERTIFICATION — the primary focus below is owned by that track and remains live: original note follows. (**🩺 HEALTH QUESTION CERTIFICATION — the certification MODEL evolved, and the reusable question-truth spine is COMPLETE.** This session progressed through one architectural arc: **Glucose Current-Context fix → a broader Truth-Exposure pattern → the reusable Reading-Series capability → reusable Trend / Comparison / Adherence capabilities → a shift from Domain Certification to QUESTION Certification → a data-driven Question Catalog → certification that self-verifies against the live capability registries.** The lesson — not the code — is what became permanent: **we certify the customer's QUESTION, not the implementation; we build ONE reusable platform capability, never per-domain logic; and certification is COMPUTED from live registries, never asserted.** Folded to `01 §5`/`§6` (the certification-model lesson) and `03 §3i` (the engineering discipline); governing doc `docs/WLJ_HEALTH_KNOWLEDGE_CERTIFICATION.md` indexed in `99 §B`. Five reusable capabilities shipped and now permanent infrastructure — **Reading Series · Trend · Comparison · Adherence · hour-of-day distribution** — plus the **Question Catalog framework** (`apps/core/truth/question_catalog.py`) and the **Health catalog** (80 questions, **74/80 certified — 92.5%**). Vitals truth (heart rate/water/SpO2/temperature history + intra-day readings) is exposed; the previously-blind Health pages are Current-Context aware. **PRIMARY FOCUS updated below to make Health Question Certification the governing model; NEXT MILESTONE = Excursion Frequency Trend (P1).** All parallel tracks + the deferred roadmap + Waiting-on-Danny preserved. Committed by explicit pathspec (concurrent foreign work — `.gitignore`, `apps/core/truth/domain.py`, Renpho/Danny_Story docs — left untouched).**) -->

> **⚠️ CoS-track note (2026-08-14):** the Health Question Certification PRIMARY FOCUS immediately below is a **different, still-live track** — leave it intact. The Chief-of-Staff Executive-Truth architecture arc this session drove is the **ACTIVE CoS ARCHITECTURE TRACK**, and its **FIRST OPEN ITEM** for the next CoS session appears in its own section (below the primary focus). If you are picking up **CoS / "how am I doing" / Executive Synthesis / heuristic-provenance** work, that section is your starting point.

<!-- PRIOR REGEN (2026-07-25): 📐 Executive Truth — constitutional ownership SETTLED (architecture review series, no code); durable conclusions in 01 §5/§6, ownership-classification discipline in 03 §3h, blueprint docs/WLJ_EXECUTIVE_TRUTH_OWNERSHIP_BLUEPRINT.md. A parallel Operations track closed the same day (Config Governance OPS-13 report-only + Recovery Stabilization f871a511; lessons 03 §4a/§7a). Both remain valid and are preserved in the sections below. -->
---

## 🧭 PRIMARY FOCUS — 🏁 CoS CERTIFICATION PROGRAM = ACTIONABLE COMPLETE (139/140); next = PRODUCT phase "helps me run my life"

> **🏁 THE WHOLE CoS CERTIFICATION PROGRAM IS ACTIONABLE COMPLETE (2026-08-14).** All 13 registered catalog domains mechanically certified (**139/140, 99.3%**; only GAP = deferred `health.body_temperature.current_context`); Health · Fitness · Medicine · Goals · Tasks · Medical · Habits · Calendar · Brain Training · People · Legacy · Projects · Notes · Capture natural-certified on Danny's real data; cross-domain Executive Synthesis grounded (prioritizes, distinguishes strengths/problems, knows what it's missing). Program-wide ledger = `docs/WLJ_COS_CERTIFICATION_LEDGER.md`. Catalog module `apps/core/truth/cos_question_catalogs.py`; ratchet `RemainingDomainRatchetTests` locks all 12. **Do NOT continue generic certification / add catalog questions to keep a score going. Do NOT reopen CoS architecture.**
>
> **➡️ NEXT PHASE = PRODUCT: "from knows-my-life → helps-me-run-my-life."** A read-only product investigation (`docs/WLJ_COS_PROACTIVE_PRODUCT_INVESTIGATION.md`) found WLJ ALREADY has a wired proactive backbone (PGS check-ins every 15 min seed real chat messages + bell, default-on; DBE dashboard card; app-open briefing) — but that proactive voice runs on the LEGACY `PersonalAssistant` pipeline, NOT the certified `model_interface` CoS; follow-through is broken (`snooze_until` unconsumed, no cross-session thread); missing-data is on-request only; a large action surface is unexposed; push-to-device is dormant (operator: needs a registered `MobileDevice` + APNs env creds). **RECOMMENDED ONE next milestone (AWAITING Danny's review — do NOT implement): the Proactive Daily Executive Brief in the CERTIFIED CoS's voice** — once-daily (app-open/morning) the certified whole-life executive synthesis opens the day (what matters / what changed / what's slipping / what I'm missing) through the already-wired proactive surface. Reuses everything; builds NO new scheduler/channel/briefing-engine/importance-brain. #2 follow-through, #3 proactive missing-data ask, #4 action completeness, #5 Reveal Target (gate-ready). See the investigation doc for the full gap matrix + guardrails.
>
> <details><summary>Prior arc detail (Health/Fitness certification — kept for provenance)</summary>

## 📓 (superseded) PRIMARY FOCUS — Health Knowledge Certification ACTIONABLE COMPLETE (82/83); CoS Domain Certification → Fitness

> **🏁 HEALTH KNOWLEDGE CERTIFICATION IS ACTIONABLE COMPLETE (2026-08-14, `fc1ea0ee`).** P1–P5 all shipped + conversationally certified. **Health catalog 82/83 (98.8%).** The ONLY remaining GAP is `health.body_temperature.current_context`, **intentionally DEFERRED** — it needs a real Temperature workspace/page that does not exist, and one was deliberately NOT built to fake 100%. Every currently **actionable** Health customer question is certified. The five reusable capabilities added this arc are permanent infrastructure — **Event Frequency · Consistency (circular) · Change-Point · HRV exposure · Ranked Entity** — consume them, do NOT redesign. **the CoS Domain Certification sequence continues (RATIFIED 5-step, `03 §3d`). **✅ FITNESS certified (`b12d5daf`, 2026-08-14) — deterministic truth + routing DONE, AWAITING Danny prod val.** All 20 fitness questions PASS by REUSING the platform: `training_volume` history (reuses canonical `WorkoutSession.total_volume` → inherits trend/comparison/change-point), `workout_by_volume` ranked_entity, `PersonalRecord` entity+analysis (Brzycki e1RM); workout/steps change-point free via cataloging. Fitness lives under `health`; completion is DERIVED, never from plans. Catalog 90/91. Live natural cert PASS. **The ranked+PR grounding fabrication ("squats/deadlifts") was ROOT-CAUSED and FIXED (`e9fe97d4`) — it was NOT a Fitness/reasoning problem but a CoS-wide Executive-Synthesis bug: `dispatch` wraps every tool result in `make_envelope` (payload nested under `value`), and Phase-2 `render_evidence`/`_facts_from_result` read the TOP level → Phase 2 received near-EMPTY evidence for EVERY wrapped tool (only freshness/confidence/source). The whole-life judgment had survived by leaning on the standing ORIENTATION; the Fitness entity question had no orientation fallback → pure fabrication. Fix = unwrap the envelope `value` + render ranked `results[]`/entity `records[]` (they were also unhandled) + ranked subjects declare `detail_keys` carrying real sub-items. Now 4/4 grounding runs name Danny's REAL workouts/exercises/PRs with real numbers, 0 fabrication; executive judgment improved (now cites resting HR/BP because evidence finally reaches Phase 2). See changelog 2026-08-14.** Do NOT rebuild Fitness truth; per-exercise strength-progression + plan-adherence are documented deferrals. **NEXT DOMAIN: Medications** (health-safety, CERTIFIED L1 reference `docs/LAYER1_DOMAIN_FRAMEWORK.md`; then Goals · Habits · People · Legacy · Calendar · Tasks · Projects · Capture · Notes · Brain Training · Medical). Do NOT reopen Health; do NOT build a Temperature workspace for 100%.

</details>

## 📓 (superseded header kept for the P1–P5 records) — Chief of Staff Knowledge Certification — Health
**The Retrieval Platform is PERMANENT CERTIFIED INFRASTRUCTURE — do NOT reopen, redesign, or re-adopt it.** The metadata contract, the authority/semantics vocabulary, `metric_date`, `get_history`/`get_entity`, Current Context, shared builders, page summaries, and Executive Context are COMPLETED, certified infrastructure (`01 §5`/`§6`). **Future architectural change requires runtime evidence of a platform defect — never proactive redesign.** Every new retrieval surface adopts the ONE convention (`authority_declarations()`/`served_keys()`/`stamp()`, or an envelope-root stamp) — no alternate mechanism.

**GOVERNING CERTIFICATION MODEL — Health QUESTION Certification (evolved this session; read `01 §5`/`§6` + `03 §3i` + `docs/WLJ_HEALTH_KNOWLEDGE_CERTIFICATION.md`).** Certification is now: **"can the Chief of Staff answer every catalogued customer question from deterministic truth?"** — never "can Claude think of more questions?" The **Question Catalog** (`apps/core/truth/question_catalog.py`; Health catalog `apps/health/health_question_catalog.py`) is the permanent standard: each question declares the truth it requires; the framework COMPUTES `certified` against the live capability registries; `python manage.py certify_questions health` renders the matrix; `test_question_catalog.py` ratchets it. **The reusable question-truth spine is COMPLETE and is permanent infrastructure — consume it, do not redesign it:** **Reading Series · Trend · Comparison · Adherence · hour-of-day distribution · Event Frequency · Consistency · Change-Point · Ranked Entity**, plus the catalog framework. **Health is at 82/83 (98.8%) — ACTIONABLE COMPLETE (see banner above).** Future Health certification = **ADD a `Question` to the catalog, then close its first-failing-layer** — do not invent question lists, do not build per-domain logic; a metric that declares `current`+`history` inherits trend/comparison/analysis for free.

**✅ P1 — Event/Excursion Frequency Trend: DONE + CONVERSATIONALLY CERTIFIED (verified 2026-08-14; shipped `30d1d4e6` as `event_frequency`).** The catalog required `event_frequency` (not a separate `excursion_frequency`); `certify_questions health` reports `health.glucose.lows_more_frequent` + `daytime_lows_frequency` + `severe_lows_increasing` + `lows_month_over_month` all **[PASS]**. `apps/core/truth/event_frequency.py` is the exact governing shape (Window → `build_reading_series` → excursion tally → `to_history_series()` → reused `HistorySeries.change()`); domain-agnostic, reuse-only, facts-not-verdicts, empty≠zero; wired via `get_event_frequency` + `glucose_event_frequency`. Live cert PASS: "are my overnight lows more frequent?" selects `get_event_frequency`, narrates the rising count-series/trend, distinguishes frequency from average, states arithmetic direction only, substantiates on follow-up. **Do NOT rebuild it.** (Definitional refinement for later, NOT a defect: a "low event" is a reading-level excursion count; episode-level merging is future work.)

**✅ P2 — Sleep Consistency: DONE + CONVERSATIONALLY CERTIFIED (`5ff90b27`, 2026-08-14).** New reusable platform capability `apps/core/truth/consistency.py` (`ConsistencyMetric`, the 5th Layer-1 numeric shape): centre, dispersion, most/least-regular, arithmetic variability change — domain-agnostic. **Midnight-safe circular statistics** for clock fields (11:50 PM ↔ 12:10 AM = 20 min), ordinary stats for durations; facts only, `empty≠zero`, one authoritative record/night. Sleep is first consumer (`sleep_consistency`); wired on the established pattern (`get_consistency` tool + `truth_consistency` discovery + `KNOWN_CAPABILITIES`). `health.sleep.consistency` GAP→PASS, **Health 79/83 (95.2%)**. Live cert PASS: "how consistent has my sleep been" → `get_consistency`, typical bedtime 11:54 PM (midnight-safe), reports spread + most/least-regular + "getting less consistent," distinguishes from average, leaves the verdict to the model. **Do NOT rebuild.**

**✅ P3 — Change-Point Detection: DONE + CONVERSATIONALLY CERTIFIED (`cb5b7fed`, 2026-08-14).** New reusable primitive `apps/core/truth/change_point.py` — segmented linear regression by residual reduction: finds the single date a trend materially shifted (day-offset x → irregular-sampling safe), reusing the `HistorySeries` rising/falling/flat rule for pre/post direction. Safeguards (NOT tuned to data): ≥8 obs, ≥3 pts + ≥5 days each side, endpoint exclusion, ≥50% residual reduction; no qualifying split → `supported:false` + reason, NEVER a fabricated date. Strength = concrete `residual_reduction`, no vague confidence label. Composes `get_domain_history` (reuse); weight first consumer, any per-day history metric capable automatically (index = history index, like Comparison). `health.weight.trend_change_point` GAP→PASS, **Health 80/83 (96.4%)**. Live cert PASS (HONEST): Danny's real weight = steady 6-month decline (135 pts) → CoS correctly says "**no supported change point**," no fabricated date, accurate freshness. **Do NOT rebuild; do NOT tune to force an interesting answer.**

**✅ P4 — HRV / Recovery: DONE + CONVERSATIONALLY CERTIFIED (`4626f846`, 2026-08-14).** EXPOSURE fix, not a new capability: canonical overnight HRV (`SleepEntry.hrv_value`, SDNN ms) already existed but was unregistered → added `HealthHistory.hrv` (per-night, NULLs excluded so missing≠zero) + registered `hrv` in history/analysis, so it inherits Trend/Comparison/Change-Point/Analysis automatically. **`DailyHealthSummary.recovery_score` = heuristic I.4 verdict — deliberately NOT exposed; OpenAI owns "recovery."** Catalog requirement corrected `hrv_recovery`→`history+trend` on `hrv` (the truth the question needs). `health.heart_rate.recovery` GAP→PASS, **Health 81/83 (97.6%)**. **No migration** (field pre-existed). Live cert PASS on real data (170 HRV values, ~6 mo): CoS reports HRV trend in ms (+13.6% rising), interprets recovery FROM the facts (no WLJ recovery verdict), substantiates + accurate freshness. **Do NOT rebuild; do NOT expose recovery_score.**

**✅ P5 — Ranked Entity Retrieval: DONE + CONVERSATIONALLY CERTIFIED (`fc1ea0ee`, 2026-08-14).** New reusable primitive `apps/core/truth/ranked_entity.py` (`build_ranking`) + REGISTRY-CONTROLLED cos-service `domain_ranked_entity.py` (`RANKING_SUBJECTS` — the model picks a declared subject KEY, NEVER an arbitrary field; NO shadow DB). Orders canonical entities by an ALREADY-authoritative measure — reuses `DomainTruth.describe()` + the domain's own value (NO recompute; test asserts value == `get_meal_totals`). Missing→excluded (never 0), real-0 kept, deterministic tie-break on canonical reference, bounded top-N, contribution %. Meal = OCCURRENCE (aggregation declared explicitly). Each ranked result carries the entity's own canonical items so "tell me about the top one" is answered from truth in hand (the occurrence ref also round-trips via `get_entity(entity_type=meal, filters={meal,on_date})`). `nutrition.meals_most_carbs` GAP→PASS, **Health 82/83 (98.8%)**. Live cert PASS: ranks Danny's real meals by carbs (Lunch Jul 31 = 161g), names the foods on follow-up, substantiates, accurate freshness, WLJ never labels a meal good/bad. Meal Intelligence UNTOUCHED (CoS only consumes). Future consumers (meal_by_protein, workout_by_calories, expense_by_amount…) = one registry entry each; NOT built. **Do NOT rebuild; do NOT make it a generic DB-query engine.**

**DEFERRED (do NOT make it the next milestone): Temperature Current Context** (`health.body_temperature.current_context`). There is no temperature workspace/page yet; implement its Current Context ONLY when Temperature becomes an active workspace with meaningful user-facing functionality.

**Also available (operator-validated increments, secondary to retrieval):** **Truth consolidation (Phase A remainder)** — collapse the three route resolvers + duplicate module-home maps + `NAVIGATION_HINTS` behind the one destination authority (`ModuleDefinition.route_name` + object `get_absolute_url` + Current Context). Phase A inc.1 (Health drift) shipped; behavior-touching → parity-test + Danny prod-validates each cut.

## 🧠 ACTIVE CoS ARCHITECTURE TRACK — Executive Truth arc IMPLEMENTED + CERTIFIED; ONE residual is the FIRST OPEN ITEM
**The deferred "Executive Truth grounding + certification" milestone was executed end-to-end this session (2026-08-14) and is largely DONE + production-certified.** Governing model, now the running behavior: *WLJ owns Danny's deterministic truth; OpenAI owns interpretation/reasoning/judgment/prioritization/advice; OpenAI decides what it needs; WLJ supplies it; OpenAI decides when it has enough; OpenAI thinks across it; Clara acts as Chief of Staff.* Shipped + certified: model-directed broad retrieval · broad-answer continuity · removed `current_action` over-steer / WLJ-side intent steering · model-directed retrieval persistence · Danny-specific grounding across framings · personalized "show-your-work" retrieval · investigation sufficiency + broad scope · **current-judgment-needs-current-evidence** (standing context ORIENTS; a current evaluative claim REQUIRES current authoritative evidence — never a 0-tool judgment from `deterministic_understanding`; certified 0/12) · **durable CoS turn lifecycle** · **bounded two-phase Executive Synthesis** (Phase 1 investigates + selects truth; Phase 2 = the SAME model, ONE bounded no-tool call over the gathered evidence + capped orientation → judgment; no tools, not judge-the-judge, no new authority, eligibility = runtime ≥2 substantive surfaces, narrow stays single-phase; fall back to grounded Phase-1 on error) · evidence-payload slimming · narrow-query regression protection · Phase-2 latency correction (single hard-bounded client call). Architecture in `01 §5`/`§6`; governing doc `docs/WLJ_COS_EXECUTIVE_SYNTHESIS_PHASE.md`; blueprint `docs/WLJ_EXECUTIVE_TRUTH_OWNERSHIP_BLUEPRINT.md` (make the system match the map; never re-derive the map or redesign the envelope). **Do NOT describe Executive Synthesis as a second assistant or a reasoning engine.**

### ✅ FIRST OPEN ITEM — RESOLVED + NATURALLY CERTIFIED (`ebd9cb67`, 2026-08-14). Core CoS architecture-hardening arc recommended CLOSED.
**The heuristic-leakage / evidence-lineage residual below was root-caused on the LIVE runtime and fixed.** Proof: the flagship *"how am I doing overall"* turn claimed mission drift + "momentum scores of 25" for missions it **never retrieved** — the only source was the standing ORIENTATION forwarded to Phase 2 (`build_orientation` shipped the missions `momentum_score=25` + `deterministic_understanding`'s `biggest_risk`/`primary_challenge`/`strategic_summary`/`momentum` verdict fields). A momentum score/band IS the progress-vs-drift judgment OpenAI must form — a deterministic number encoding an I.4 verdict; handed one pre-decided the model narrated it as its own with no lineage to defend on challenge. **Smallest fix (`synthesis.py` only): hand Phase 2 FACTS, never a verdict** — `build_orientation` strips `_VERDICT_KEYS` from mission/action facts and no longer forwards the `deterministic_understanding` verdict block; `render_evidence` drops verdict labels while keeping numeric facts; the synthesis prompt reassigns ALL judgment to OpenAI. Lineage stays deterministically re-retrievable (no new persistence). Natural cert (Overall→Evidence→Freshness→Challenge→Action) PASS on the trust-critical axis: no "25" narration; the challenge turn SUBSTANTIATES from re-retrieved facts (SOFT_EXPIRED task, 0%/50% milestone) instead of repeating; accurate freshness; single mission-connected action. **Honest residual = category-4 model/prompt reasoning quality (NOT core architecture): the challenge turn re-confirms one-sidedly from weak-area evidence without weighing strengths.** Architecture is sound — future CoS work classifies as truth/context/action completeness, reasoning-quality, or product refinement, not core-architecture redesign. Changelog 2026-08-14; blueprint §4 prediction confirmed. AWAITING Danny's real-world validation.

<details><summary>Original residual (now resolved — kept for provenance)</summary>

Natural product testing right after certification exposed a **trust-critical** residual. Danny asked *"How am I doing overall in my life right now?"* Clara answered judgment-first (good) but claimed he is **"drifting rather than progressing,"** cited **"momentum scores of 25"** for Serve Others / Relationship with God, called an overdue foundational WLJ task evidence of **lack of focus/follow-through**, asserted an **"absence of any notable advancements in health, nutrition, faith, finances, or relationships,"** and said the conclusion was **"medium confidence analysis across these domains."** When Danny then asked Clara to **walk through the evidence and freshness behind each claim**, Clara did NOT substantiate — it reasserted the drift conclusion, repeated the momentum/overdue interpretations, and **quietly dropped** the "no notable advancement" claim rather than supporting it.

**Do NOT assume the Executive Synthesis architecture is wrong.** Investigate two closely-related questions, in order:
1. **Truth ownership / heuristic leakage (the I.3→I.4 boundary).** Trace whether Phase 2 is reasoning from *current deterministic evidence* or whether **legacy WLJ heuristic verdict fields are leaking into the evidence/orientation and being adopted as OpenAI's judgment.** For each of `momentum=25`, "stagnating", "drifting", `primary_challenge`, `biggest_risk`, "foundational", "medium confidence", "lack of focus/follow-through", classify: **A** canonical deterministic fact/metric (clear definition) · **B** deterministic WLJ calculation · **C** legacy heuristic interpretation/verdict that belongs to OpenAI under I.4 · **D** OpenAI inference from deterministic evidence · **E** unsupported assertion. A number being *deterministic to compute* does NOT make it constitutionally appropriate *evidence* for an executive judgment. Revisit the **Executive Truth Ownership Blueprint** and the I.3→I.4 line (the blueprint already predicted this "WLJ pre-decides I.4 verdict fields" gap).
2. **Evidence lineage across Executive Synthesis.** Determine whether enough evidence **identity/provenance/freshness** survives Phase 1 → Phase 2 for follow-ups ("what evidence are you basing that on?", "how current is that?", "why?"). Trace: is Phase-2 evidence persisted/recoverable, or only final prose? Does Conversation State keep the assessment but lose lineage? Can a follow-up reconstruct the underlying evidence? Did the **compact evidence renderer** (`synthesis.render_evidence`, deliberately slimmed for latency) discard freshness/provenance needed later? Is the model substituting `deterministic_understanding`/heuristics when lineage is unavailable? **Do NOT** solve by fabricating citations or repeating the conclusion.

**Then** make the smallest correction runtime evidence justifies, and re-run naturally: *"How am I doing overall in my life right now?"* → *"Walk me through the evidence behind that conclusion. For each important claim, what actual WLJ information supports it and how current is it?"* — the answer must cleanly separate **FACT** (what WLJ says) · **CALCULATION** (what WLJ deterministically computes) · **JUDGMENT** (what OpenAI concludes) · **UNCERTAINTY** (what cannot be established). Product standard: a CoS judgment must pass BOTH the **TRUTH TEST** ("what current authoritative WLJ evidence supports this?") and the **REASONING TEST** ("what does OpenAI think it means?") — WLJ must not pre-decide the second; OpenAI must not invent the first.

</details>

**Do NOT return to:** hardcoded domain bundles · deterministic life scores as executive verdicts · WLJ-generated "biggest concern" · phrase-list intent classifiers · generic hallucination detectors · sentence-level validators · Executive Reasoning Engine · cross-domain deterministic judgment engines · preloading all truth · zero-tool current judgments from standing context.
**Do NOT undo:** model-directed retrieval · grounding · retrieval persistence · scope completeness · the current-evidence requirement · bounded Executive Synthesis · durable turn lifecycle · narrow-query efficiency.
**Deploy-verify note (learned the hard way, `03 §11`):** the CoS runs in `wlj-worker`; after every push verify web `/_health/` **and** the worker/beat commit; and after any temp-infra removal prove the commit actually contains the cleanup (a failed multi-path `git add` can commit only a deletion → orphaned import → full-site 502).

**EXPLICITLY DEFERRED (retrieval platform + overview-certification gates are now BOTH satisfied; these come after Knowledge Certification — do NOT start early):** **bidirectional Current Context → Desired Context → Reveal Target → client presentation adapters** (the one `applyPresentation(directive)` per client that turns a target+relation into a native verb). Design is settled in `docs/WLJ_COS_PLATFORM_EVOLUTION_INVESTIGATION.md`; implementation waits.

**Guardrails (verbatim intent):** do NOT build a "Presentation Resolver" subsystem, a navigation action, Desired Context, Reveal Target, or client adapters yet; do NOT create multiple assistants, duplicate conversation systems, or domain-specific AI engines; do NOT re-own truth; **reuse-first, composition over duplication, expose existing truth before inventing.** Preserve everything already built.
**⚠️ Pre-existing (NOT this milestone):** 7 `apps.dashboard_v3` test failures from the concurrent Journal-Workspace/Dashboard redesign (`eb7392ed`) — a spawned task tracks them; confirm they're fixed independently.

## 🎯 Parallel tracks (other sessions may own — reconcile which you're driving; the primary focus above wins)
- **CoS Domain Certification** (RATIFIED 5-step, `03 §3d`). Nutrition ✅ Journal ✅ prod-complete; Faith close-out (round 2) shipped, AWAITING prod validation. **Next domain: Fitness**, then Medications · Goals · Habits · People · Legacy · Calendar · Tasks · Projects · Capture · Notes · Brain Training · Medical.
- **Timestamp Precision Phase 2/3** (`docs/WLJ_TIMESTAMP_PRECISION.md`; principle `03 §7`). Phase 2 = persist `observed_precision` per-domain, certification-gated (Health first — 8 noon-fabricating ingest sites). Phase 3 = presentation adopts. **Do NOT big-bang it.**
- **Conversation State migration inventory (audit only)** — retire the two non-prod runtimes' duplicate conversation systems; gated on runtime consolidation (all users → `model_interface`). Durable records stay put.
- **WLJ Operations — Configuration Governance & Recovery.** Foundation COMPLETE this session: Configuration Governance **OPS-13** (report-only drift detection, deployed prod-verified `43e3c128`; architecture RATIFIED `4639a4f2`), the 2026-07-23 truth-path **investigation** (`e2980b40`→`b858e060`), and **Operations Recovery Stabilization** (`f871a511` — hysteresis + `RECOVERING` state + alignment-badge relabel; durable lessons folded to `03 §4a`/`§7a`). **NEXT = OPS-14 Configuration Visibility** — a pure read-only Ops Wall card rendering the existing `config_integrity` section's own status (decoupled from global status, no enforcement); **approval-gated, NOT started.** Then OPS-15 (config→operations state + CoS banner, medium blast) · OPS-16 (enforcement, high — report-only first) · OPS-17 (continuous). Milestone map: `docs/WLJ_CONFIGURATION_GOVERNANCE.md §10`. The latent two-authority *risk* (COAS vs Integrity) is documented, was **NOT** this incident's cause, and consolidation is deferred until evidence warrants — do NOT preemptively consolidate.
- **Other parallel owners (do not collide):** Rich Confirmation · Structured Import Orchestration · Configuration Governance. **(Meal Intelligence is now foundationally complete — see its own section below.)**

## 🍽️ Meal Intelligence — foundationally COMPLETE; continue by REFINEMENT, never redesign
**Status:** a mature standalone deterministic truth domain (Foundations 1–2 + Ingredient Intelligence shipped 2026-07-20). Durable knowledge + the strategic direction are now permanent in `01 §5`/`§6` — **read them there, don't restate.** In one line: **Meals owns food truth; the CoS will *consume* it (never own it); future meal/grocery/pantry/recipe/nutrition conversations flow through the CoS once the CoS platform stabilizes — do NOT redesign Meals around today's CoS.** Governing docs: `docs/WLJ_MEAL_INTELLIGENCE_ARCHITECTURE.md`, `WLJ_INGREDIENT_INTELLIGENCE.md`, `WLJ_MEAL_INTELLIGENCE_TRUTH_CERTIFICATION.md`, `WLJ_MEAL_INTELLIGENCE_ROADMAP.md`.

- **AWAITING Danny's real-world validation (all shipped this session):** Foundation 2 lifecycle (prep → deduction → leftovers → consumption → nutrition → waste), Pantry **Container Truth** + **Remaining Truth**, **Pantry Smart Search**, **Manual Pantry Entry**, **Ingredient Intelligence** (+ curated-aliases-no-runtime-learning refinement), the single **Pantry Availability authority**, and the three product-defect fixes (availability drift · blank substitution names · meal score of 0). *Implemented · deployed · certification passing — but NOT validated until Danny confirms in production.*
- **Remaining implementation (carry forward — product refinement + remaining capabilities, NOT redesign):** continued real-world product validation · UX improvements found in testing · Ingredient Intelligence refinement where needed · Pantry usability + matching improvements · Manual Pantry Entry refinements · Container Truth polish · **meal planning intelligence · grocery list generation · shopping workflows** · pantry optimization · **price intelligence · store intelligence** · future grocery-ordering integrations · analytics/reporting enhancements · any deferred product refinements from this session. (Roadmap M4+.)
- **Sequencing:** these continue after the CoS platform work reaches stability; they remain Meal Intelligence responsibilities and integrate as a platform *consumer* (`01 §6`).

## 🔮 Deferred / carried (open as its own step before implementing)
- **Journal Workspace (Focus Compose) — SHIPPED 2026-07-21 (`f9b93c81`), AWAITING Danny's prod validation; refinements carried.** The Journal editor is the writing-first Focus Compose workspace (editor owns the screen + sticky Compose Dock: always-visible mood/tags/categories chips → lightweight overlay pickers, live-update, zero layout shift; in-dock Save/Cancel). Presentation-only — POST/model/migration unchanged; verified real-page desktop+mobile in Chromium. Governing doc `docs/WLJ_JOURNAL_FORM_LAYOUT.md`; platform pattern `01 §6`; front-end invariants + interaction-redesign discipline `03 §6a`. **Carried refinements (visual polish, not architecture — do NOT reintroduce viewport math or a second scroll owner):** refine the Compose Dock visual design · slightly widen the editor on desktop · strengthen the dock's visual hierarchy · increase emphasis of the primary Save action · slightly strengthen the selected-metadata chips · **revisit the presentation of the three Journal modes** (Just Write / Write Together / Talk It Through) *after* the dock is complete · **generalize the Compose Dock into a reusable WLJ Workspace Dock** (Health/Travel/Faith metadata) only AFTER the Journal implementation is complete (platform-consumer pattern, `01 §6`). **Safari/WebKit validation is on Danny (below).**
- **Travel Intelligence — DESIGNED this session, NOT the next milestone.** Full design in `docs/WLJ_TRAVEL_INTELLIGENCE_ARCHITECTURE.md` (v0.1 DRAFT); governing pattern in `01 §6`. It is a **platform-consumer showcase**, built only *after* the reusable platform capabilities above exist. **One open decision blocks ratification (see Waiting on Danny): classify Travel `BEHAVIORAL` (recommended) vs the reserved `CONTEXT` (`descriptors.py:26`).** Do NOT begin Travel code; do NOT build a Travel AI/conversation/reasoning engine. Recommended MVP when it's time: truth spine + conversational planning (Phases 0–2); GPS/live mode is Phase 3 (greenfield — zero device location exists today). Carried sub-concepts: **Workspace ≠ Session**; **location truth = shared platform capability** (Travel = first consumer, not owner).
- **Journal polish that needs Danny's LIVE model + real mic to advance & validate** (already substantially built — do NOT blindly re-tune what you can't run/hear): deeper **contextual reasoning** (relationship reasoning over today's truth), deep **voice polish** (speaking/thinking transitions, mic recovery, reconnect, endpointing feel), **conversation quality** ("feels like my Chief of Staff, not AI"). Voice Pause naming vs the durable draft is minor polish (the transcript already persists every turn → Resume Talk It Through is durable).
- **Journal recent-context truth:** lightweight, request-path-safe "today" facts (meals/exercise/glucose/meds) to deepen questions — needs a cheap cached per-domain snapshot, not heavy builders.
- **Journal genuinely-new-truth:** "goals discussed in my journal", "people mentioned most in my journal" — require NEW deterministic truth, not exposure.
- **Timestamp Precision Phase 2/3** and remaining Health precision rollout · **Artifact perception adapters** (PDF/DOCX/OCR — extend *perception* only, not Conversation State) · **Faith refinements** (pickers, reminder UI, Collection model, Mirror theme analysis) · **WLJ Certification Platform** remaining types · **UTC-vs-user-local calendar-day attribution** · **WLJ Operations** controlled production enablement of the ship-dark recovery engine (Phase III, evidence-gated → OPS-8a) — distinct from the Configuration Governance / OPS-14 track above.
- **Long-term product idea ONLY — tabled, NOT active:** Faith **Life Seasons / Life Chapters**. Do not open without Danny's explicit go.
- **CLOSED — do not reopen:** Renpho direct / Terra.

## ⏳ Waiting on Danny (operator — Claude has no prod access)
- **RATIFY Travel classification (one decision, unblocks the design):** classify Travel Intelligence `BEHAVIORAL` (recommended — a first-class life domain the CoS serves) vs the reserved `CONTEXT` placeholder. In-Constitution; product call, not a Constitutional Review.
- **Validate in production:** the **Journal unified-draft lifecycle** (M-D1…M-D4, shipped 2026-07-20, flag `features.journal.write_together`, owner-only — journal by talking then typing then talking across a day; confirm the draft follows you and nothing is lost; Finish & Review → Save Journal reads as one story from both channels), **Conversation State**, **Faith domain certification**, the **Measurement Session Capture confirmation experience** (hardening pass shipped 2026-07-20 — upload a body-measurement screenshot → the RESULTS-not-intentions confirmation lists recognized / skipped + why / counts, nothing dropped silently → confirm → the check-in appears in Body Intelligence and is answerable by the CoS; the *core* capability was already validated 2026-07-19, this validates the new confirmation + the deterministic "Import" persistence), plus still-open Faith First Light + Health Sync validations.
- **Validate the Journal Focus Compose workspace in Safari/WebKit (the iOS app + Safari) — the one surface not drivable locally.** Confirm on Create AND Edit: the page scrolls normally (one scrollbar), the editor never resizes/collapses when opening a metadata picker or selecting a mood/tag/category, chips always show current values and update live, nothing is clipped, Save/Cancel stay reachable, and it holds across desktop/mobile + browser zoom. Shipped `f9b93c81`; design is plain document flow + overlays + the shell's own scroll (the safest WebKit structure), but only on-device confirms it.
- **Validate in production: Operations Recovery Stabilization** (`f871a511`, deployed). During the next real degradation confirm the CoS banner / header dot / Ops Wall no longer flap 🟡→🟢→🟡: a `RECOVERING` amber state shows during the confirm window, "recovered" fires **once** only after sustained stability with no active P1/P2 incident, and the header badge reads **"Alignment"** (labelled — not "Status", not a bare green "100%") and renders **"—"** if alignment can't compute. (Hard to force locally — needs a real production dip.)
- **Deploy topology:** the CoS runs in **`wlj-worker`**; `/_health/` reports only the web commit — verify the worker is on the tested commit before trusting a production CoS result.

## 🔀 Concurrency — coordinate, do not collide (Danny runs MANY parallel sessions on the SAME tree)
Commit **only your own files by explicit pathspec** (`git commit -m … -- <paths>`). The changelog and shared files (`apps/core/truth/domain.py`, `apps/ai/multimodal.py`, `apps/ai/cos_services/*`, this bootloader) are heavily contended: re-check the changelog top **immediately before each commit**, defer your line if a foreign entry appeared, isolate your hunk from foreign uncommitted work, verify the push is a fast-forward (never lose a foreign commit). A concurrent session may push the shared branch — including *your* commit — so "nothing to push" can be normal; re-fetch and confirm your SHA is on remote.
