# 99 · Reference Index — master table of contents

**Responsibility of this document:** the map. Every governing document — where it is, its authority, who it's for, whether it's current or historical, and what it's for. The startup package is the onboarding experience; the supporting library is the detail. Read the startup package; reach into the library on demand.

Legend — **Authority:** Constitutional / Governing / Reference / Operational. **Audience:** All (ChatGPT+Claude+Danny) / Engineer / Operator. **Status:** CURRENT / HISTORICAL.

---

## A. The startup package (drag in this ONE folder; read in order)

| # | Title | Authority | Audience | Status | Purpose |
|---|---|---|---|---|---|
| 00 | NEXT CHAT STARTUP | Operational | All | CURRENT (regenerated) | **START HERE** — the bootloader. Live sprint state only; the one temporary file; shrinks over time. Points you at the rest. |
| 01 | READ FIRST — Architecture | Governing | All | CURRENT | **WHAT WLJ is** — vision, the OpenAI pivot, current architecture & maturity, lessons. |
| 02 | WLJ Constitution | **Constitutional** | All | CURRENT | **WHAT MUST NOT CHANGE** — protected Articles + Constitutional Review (default NO). The apex. |
| 03 | Engineering Operating Guide | Governing | Engineer | CURRENT | **HOW TO BUILD SAFELY** — tracing, root-cause proof, gates, deploy/doc discipline. |
| 04 | Danny Working Preferences | Governing | All | CURRENT | **HOW TO WORK WITH DANNY** — communication, workflow, product/investigation/decision/life philosophy. |
| 98 | Session Transition Protocol | Governing | All | CURRENT | **HOW TO CLOSE A CHAT** — the doctrine (executed by root `99_PREPARE_NEXT_CHAT.md`). |
| 99 | Reference Index | Reference | All | CURRENT | **WHERE EVERYTHING IS** — this file. |

*The package is self-contained: dragging in `00_WLJ_CHIEF_OF_STAFF_STARTUP/` is all a new session needs. Only `00_NEXT_CHAT_STARTUP.md` is temporary; the other six are evergreen.*

## A2. End-of-chat executor (at the `@WLJ_SYSTEM_PROMPTS/` root — NEVER loaded into a new chat)

| File | Authority | Audience | Status | Purpose |
|---|---|---|---|---|
| `99_PREPARE_NEXT_CHAT.md` | Operational | All | CURRENT | Dropped at the **end** of a working chat. Runs the transition, produces the Transition Audit, rewrites `00_NEXT_CHAT_STARTUP.md`. Permanent but action-oriented — that's why it stays outside the package. |
| `00_README_LOAD_MANIFEST.md` | Reference | All | CURRENT | Library load-guidance + the permanent startup/transition workflow diagram. |

## B. Supporting library — vision, contract, laws (authoritative detail, in `docs/`)

| Title | Authority | Audience | Status | Purpose |
|---|---|---|---|---|
| `WLJ_PRODUCT_VISION.md` | Governing | All | CURRENT | Highest-level "why": Personal Truth Platform; the model reasons, WLJ knows. |
| `WLJ_LLM_TRUTH_ACTION_CONTRACT.md` | Governing | Engineer | CURRENT | The truth/action/preference boundaries in detail (Article I). |
| `WLJ_ARCHITECTURE_LAWS.md` | Governing | Engineer | CURRENT | Answer Precondition Pipeline (Laws 0–5). |
| `WLJ_CONDUCTOR_DEVELOPMENT_MODEL.md` | Governing | Engineer | CURRENT | Layered development model (Truth→Reasoning→Action→Experience). |
| `WLJ_CURRENT_CONTEXT_CONTRACT.md` | Governing | Engineer | CURRENT | Current Context two-pattern standard (Article II). |
| `WLJ_COS_PLATFORM_EVOLUTION_INVESTIGATION.md` | Governing (roadmap) | Engineer | **CURRENT — architecture ESTABLISHED; implementation roadmap** | **The CoS-platform architecture record + certified implementation roadmap (2026-07-20/21).** The full arc: "one CoS, many workspaces" → navigation/presentation are *not* the abstraction → **"reveal a target" via *bidirectional* Current Context** (terminal grammar `{object, summary}`; workspace/capability are aliases; `already_visible = target_ref==current_ref`; the presentation **verb** lives at the client adapter, never WLJ) → **Current Context Certification** (the named milestone between Truth and bidirectional CC) → **certification precedes expansion.** Records the elimination audit (34 route-decision sites → one destination authority) and the shipped increments: Health module-home drift fix + `test_module_home_authority`; `journal.home` + medication/legacy-story object CC; the request-path-safe **Dashboard Day Summary** (`summary:dashboard.day`, SAE-snapshot). **Read FIRST for any CoS-platform / Current-Context-coverage / navigation / reveal-target work** — it says what is settled and what to build next. Architecture folded into `01 §6`; disciplines in `03 §1a`. |
| `WLJ_COS_PROACTIVE_PRODUCT_INVESTIGATION.md` | Governing (product record) | Engineer | **CURRENT — Proactive Product Phase 2 COMPLETE (2026-08-17); §22 = as-built** | **The "knows my life → helps me run my life" product record.** Investigation + the shipped Phase 2: **Daily Executive Brief · Durable Follow-Through · Missing-Data Intervention · Action Completeness** — each reuses the certified `model_interface` CoS + the existing PGS/DNE delivery; no new scheduler/channel/importance-brain. §22 is the as-built + natural-cert results. Read for any proactive / follow-up / missing-data / action-exposure CoS work. |
| `WLJ_OPENAI_COST_AUDIT.md` | Governing (cost) | Engineer/Operator | **CURRENT — audit + governance milestone SHIPPED (2026-08-16)** | **The OpenAI cost record + the accounting architecture.** Forensic attribution of the Aug-2026 surge (Claude's own `cos-run` cert testing × the Executive-Synthesis multiplier) + the ONE accounting seam (`apps/ai/llm_accounting.py::record_llm_event` → `owner_finance.LLMUsageEvent`, `source`+`traffic_class`), the generate-before-suppress fix, measured gpt-4o unit costs, and the Tier-1…4 testing discipline (mirrored in `03 §10a`). **Read before any real-model testing or CoS-runtime cost work.** |
| `WLJ_COS_CERTIFICATION_LEDGER.md` | Reference (program record) | Engineer | **CURRENT — CoS Domain Certification program ACTIONABLE COMPLETE (139/140)** | **Program-wide certification ledger** — the 13-domain catalog (`apps/core/truth/cos_question_catalogs.py`, ratchet `RemainingDomainRatchetTests`), per-domain natural-cert status, and the "expose-before-invent / reuse the platform" discipline. Read to see what the CoS is certified to answer; do NOT reopen to keep a score going. |
| **Retrieval Platform (certified — the doc set)** | Governing → Reference | Engineer | **CURRENT — platform CERTIFIED, adoption COMPLETE (2026-07-24); permanent infrastructure** | **The Retrieval Authority arc's governing record, read in order: `WLJ_RETRIEVAL_AUTHORITY_AUDIT.md` (the acceptance criteria — inventory + 5-way classification of every retrieval surface) → `WLJ_RETRIEVAL_PLATFORM_CERTIFICATION.md` (verdict + the F0 metadata-contract blocker, now closed) → `WLJ_PLATFORM_ADOPTION_ROLLOUT.md` (the FINAL surface matrix + the permanent adoption convention) → `WLJ_CANONICAL_SAFETY_MIGRATION.md` (safety is owned by the canonical layer, not projections).** Implemented by `apps/core/truth/authority.py` (the metadata contract) + `test_retrieval_authority_contract.py` (the build-gate ratchet). Records: `WLJ_TRUTH_RETRIEVAL_CERTIFICATION_PROGRAM.md`, `WLJ_RETRIEVAL_AUTHORITY_VERIFICATION.md`, `WLJ_RETRIEVAL_AUTHORITY_CLOSURE.md`, `WLJ_WEIGHT_YESTERDAY_INVESTIGATION.md`, `WLJ_NUTRITION_PROTEIN_INVESTIGATION.md`. **Architecture folded into `01 §5`/`§6`; the platform is consumed, not redesigned.** Read before any retrieval-surface / authority-declaration / new-domain-adoption work. |
| `WLJ_TRUTH_RETRIEVAL_SELECTION_INVESTIGATION.md` | Investigation (evidence + decision) | Engineer | **CLOSED — implementation SHIPPED in the Retrieval Platform arc (2026-07-24); superseded by the doc set above** | **Runtime-proven record of the "protein yesterday" trust failure (2026-07-21).** The deterministic truth existed (`get_history(nutrition,protein,yesterday)`→134 g); the model never called it — the `ToolCallLog` trace (reproduced 4/4 through the real `model_interface`+gpt-4o path) shows it reached `get_foundational_health_facts`, a **hand-curated key-value facts list that shadows the systematic `get_history` authority** with an incomplete/asymmetric key set (`calories_yesterday` exists; `protein_yesterday` absent, `protein_today` broken) and returned a false "not available." The class: **a parallel deterministic authority violating one-authority-per-domain.** Smallest fix: retire/delegate the overlapping metric keys + a single-authority summary-retrieval certification gate. Lessons folded into `01 §6`; investigation discipline in `98 §4b`. Residual: the `ToolCallLog` ledger has no operator read channel. Read when the Truth Retrieval Certification track evaluates the broader retrieval architecture. |
| `WLJ_EXECUTIVE_TRUTH_OWNERSHIP_BLUEPRINT.md` | Governing | Engineer | **CURRENT — the definitive Executive Truth ownership map (2026-07-24); ownership SETTLED, remaining work is grounding/adoption/certification** | **The constitutional ownership blueprint future Executive Truth implementation follows.** Decides, per artifact and per field, "who constitutionally owns this?" by Articles I.3 (WLJ — deterministic scores/classifications/counts) vs I.4 (OpenAI — verdicts/prioritization/narrative). Establishes: the machinery already exists (`interpret()`/`ExecutiveSignals`, Deterministic Understanding, the Executive Context Envelope is **architecturally complete**); `ExecutiveSignals` is Mixed and splits cleanly (keep `workload`/`cognitive_load`/`recovery_needed`/health-trend in WLJ; relocate `biggest_risk`/`priority_action`/`primary_challenge`/`executive_picture`/patterns-as-meaning to the model); the two truth stacks (certified authorities vs the heuristic `Insight`/`Prediction` pipeline) share no code; the transition needs **no new architecture and no Constitutional Review** — the Constitution *is* the spec. Evidence trail (historical, read for the reasoning behind the map): `WLJ_TRUTH_LAYER_EXECUTIVE_ASSESSMENT.md` · `WLJ_TRUTH_FRONTIER_REVIEW.md` · `WLJ_EXECUTIVE_TRUTH_TRANSITION_REVIEW.md` · `WLJ_EXECUTIVE_LAYER_CONSTITUTIONAL_AUDIT.md`. Architecture folded into `01 §5`/`§6`; the ownership-classification discipline in `03 §3h`. Read FIRST for any executive/cross-domain/"how am I doing"/whole-life-reasoning grounding or certification work. |
| `WLJ_COS_EXECUTIVE_SYNTHESIS_PHASE.md` | Governing | Engineer | **CURRENT — shipped + production-certified 2026-08-14; one residual OPEN (first active CoS item)** | **The bounded two-phase Executive Synthesis architecture** — how a broad "how am I doing?" judgment is produced: ONE Chief of Staff, TWO phases of ONE task. Phase 1 (the tool loop) investigates + selects WLJ truth and captures the gathered evidence; Phase 2 = the SAME model, ONE bounded no-tool completion over that evidence + capped standing orientation → the judgment. Phase 2 never sees Phase 1's prose (NOT judge-the-judge), introduces no authority/verdict/score, falls back to the grounded Phase-1 answer on error. Eligibility = a runtime signal (≥2 independent substantive truth surfaces), never a phrase classifier; narrow questions stay single-phase. Records the current-evidence rule (standing ORIENTS; a current evaluative claim REQUIRES current authoritative evidence — no 0-tool judgment from `deterministic_understanding`), the durable-turn-lifecycle work, evidence-payload slimming, the single-hard-bounded Phase-2 call (latency), and the OPEN residual: heuristic-verdict leakage + evidence lineage for "show me the evidence" follow-ups (the I.3→I.4 boundary). Architecture folded into `01 §5`/`§6`; read WITH `WLJ_EXECUTIVE_TRUTH_OWNERSHIP_BLUEPRINT.md`. Read FIRST for any Executive Synthesis / whole-life "how am I doing" / heuristic-provenance / evidence-lineage work. |
| `WLJ_CONVERSATION_STATE_ARCHITECTURE.md` | Governing | Engineer | **FROZEN — awaiting prod validation** | **Conversation State — the deterministic authority for "what are we talking about / doing / waiting on" (distinct from Current Context's "what page").** Conversation-scoped (`AssistantConversation.metadata["conversation_state"]`); tracks active subject/artifacts (+ artifact re-delivery) + reads-through pending confirmations; carried INSIDE the Executive Context Envelope; salient lead in `ModelInterfaceService`. WLJ owns the state; the model reasons over it (constitutionally compliant, no Review). **Governance frozen 2026-07-20:** §4a Deterministic Writers · §4b/§4b.1/§4b.2 artifact re-delivery + adapter separation + coverage · §5a Permitted Data · §5b Expansion Test. Enforced by `test_conversation_state_{writer,schema}_contract.py`; durable rules in `03 §3e/§3f`. Read before any follow-up/pronoun/pending-confirmation/active-artifact continuity work; extend ONLY via the Expansion Test. |
| `WLJ_VISUAL_TRUTH_CONTRACT.md` | Governing | Engineer | CURRENT | Only real completion may look complete (Article V.1). |
| `WLJ_EXECUTIVE_REFLECTION_ARCHITECTURE.md` | Governing | Engineer | CURRENT | Reflection observes, never overrides. |
| `LAYER1_DOMAIN_FRAMEWORK.md` | Governing | Engineer | CURRENT | How a new canonical truth domain is built (Article III.1). |
| `WLJ_MEAL_INTELLIGENCE_ARCHITECTURE.md` | Governing | Engineer | CURRENT | **Meal Intelligence domain v1.0** — canonical food-lifecycle architecture. Meals owns operational recipe truth (Legacy projects meaning); supply is household-scoped, consumption/health person-scoped; two authoritative ledgers (inventory, consumption); *Capture Once, Reuse Everywhere*. Read before ANY meal/nutrition/pantry/recipe/shopping work. |
| `WLJ_MEAL_INTELLIGENCE_TRUTH_CERTIFICATION.md` | Governing | Engineer | CURRENT | Certification standard for each food truth — 7 gates, M0–M5 maturity ladder, per-truth register (owner/primary-truth/maturity/criteria/representative deterministic questions). The bar Meal Intelligence must clear to be a certified truth domain. |
| `WLJ_MEAL_INTELLIGENCE_ROADMAP.md` | Reference | Engineer | CURRENT | Implementation milestones (M1 consolidation → M2 recipe enrichment → M3 lifecycle spine → M4 supply intelligence → M5 automation → M6 external). Foundations first (recipe-structured-at-write; prep/consume spine). No time estimates; **Foundations 1–2 shipped**; remaining = M4+ product refinement. |
| `WLJ_INGREDIENT_INTELLIGENCE.md` | Governing | Engineer | CURRENT | **Canonical Ingredient identity (2026-07-20).** The single deterministic authority for "are two names the same real-world ingredient?" — `normalized_name` key (case/punct/plural) + curated aliases, resolved through `ingredient_intelligence.resolve_ingredient`; every acquisition path converges on it. **No fuzzy, no AI; aliases are curated, never runtime-learned.** Identity (same thing: bun/buns) is distinct from substitution (interchangeable: beef patty/ground beef; 2%/whole milk) — never merged. Read before any ingredient-matching/dedup/search work. |
| `WLJ_TRAVEL_INTELLIGENCE_ARCHITECTURE.md` | Governing (proposed) | Engineer | **DRAFT — awaiting Danny's ratification; not built** | **Travel Intelligence domain design + product vision (2026-07-21).** The first domain designed as a **platform consumer / showcase** of "One Chief of Staff, many workspaces" — NOT a parallel architecture, NO Travel AI. Lifelong domain owning deterministic travel truth (trips/drafts/routes/reservations/packing/sessions/GPS/mileage/maintenance/expenses/memories/stories/preferences), exposing truth · **many workspaces** · actions · Current Context; the one CoS reasons over it. Defining principle **Compose, Don't Duplicate** (links/projects People·Health·Journal·Finance·Calendar·Meals·Faith·Legacy; never re-owns). Two carried concepts: **Workspace ≠ Session** (workspace forever; Session only while traveling) and **location truth as a shared platform capability** (Travel = first consumer, not owner). **One open decision (Danny):** classify Travel `BEHAVIORAL` (recommended) vs the reserved `CONTEXT`. In-Constitution (no Review). **NOT the next milestone** — platform evolution comes first. Governing direction folded into `01 §6`. |
| `WLJ_MODEL_INTERFACE_DESIGN.md` | Reference | Engineer | CURRENT | The Model Interface seam + executive context envelope. |
| `WLJ_TRUTH_SURFACES.md` | Governing | Engineer | CURRENT | **Validated as-built catalog of the 7 deterministic Truth Surfaces** the CoS reasons from (Current/Standing Context, Personal Truth, DomainTruth, Domain Entity, Executive Briefings, Decision Authority). Complementary not competing. Key principle: a missing provider changes WHICH surface answered, not whether it's possible. Read before assuming a "gap." |
| `WLJ_CERTIFICATION_BACKLOG.md` | Governing | Engineer | CURRENT | **The certification-driven operating model + evidence-ranked backlog** (measured vs NOT-YET-MEASURED). The roadmap driver: certification, not intuition. Companion: `WLJ_CUSTOMER_TRUTH_CERT_PROD1.md` (production scorecard), `WLJ_CUSTOMER_TRUTH_CERT_SLICE1.md` (local slice), `WLJ_TRUTH_RETRIEVAL_CERTIFICATION_AUDIT.md`, `WLJ_TRUTH_RETRIEVAL_COVERAGE.md`. |
| `WLJ_HEALTH_KNOWLEDGE_CERTIFICATION.md` | Governing | Engineer | **CURRENT — the governing Health certification model; the Question Catalog IS the standard (2026-07-30)** | **Certify QUESTIONS, not domains.** The permanent, data-driven certification artifact: the Health Question Catalog (`apps/health/health_question_catalog.py`) declares the real customer questions; the reusable framework (`apps/core/truth/question_catalog.py`) COMPUTES `certified` by checking each question's required truth against the LIVE capability registries (a question auto-certifies when its surface ships; a ratchet test locks the certified set; `python manage.py certify_questions <domain>` renders it). Documents the reusable platform capabilities (Reading Series · Trend · Comparison · Adherence · hour-of-day distribution), the current matrix (**74/80 certified**), the questions still impossible with exact reasons, and the phased backlog (P1 Excursion-Frequency Trend → P2 Sleep Consistency → P3 Change-Point → P4 HRV/Recovery → P5 Ranked-Entity; Temperature CC deferred). Read FIRST for any Health-question / reusable-capability / certification work. Architecture folded into `01 §5`/`§6`; engineering discipline in `03 §3i`. Domain-agnostic — future domains register their own catalog. |
| `WLJ_COS_DOMAIN_CERTIFICATION_STANDARD.md` | Governing | Engineer | CURRENT | **The RATIFIED 5-step per-domain completeness process (2026-07-19), extracted from Nutrition + Journal (both prod-complete).** (1) verify deterministic truth → (2) expose existing truth (exposure precedes new truth) → (3) validate conversational routing (routing is a separate layer; drift-proof metadata from ONE source) → (4) Danny production validation (the gate) → (5) close. Invariants: WLJ never renders a verdict; retrieve/search/analyze are distinct discoverable tools. Also folded into `03 §3d`. Run for every future CoS domain (Faith next). |
| `WLJ_TRUTH_VALIDATION_CENTER.md` | Governing | Engineer | CURRENT | **The deterministic CoS-vs-WLJ validator (Owner-2 instrument), built 2026-07-19.** One engine typed by `validation_type` (extends the Acceptance engine — no parallel framework); resolves each object by the app's own selection rule (visible resolution card); grades the CoS answer against WLJ truth deterministically (no model grades a model); classifies failures by first-failing-layer into an executive category breakdown; resolved/natural prompt modes. Read before ANY CoS certification/validation/truth-comparison work. **Altitude caveat:** certifies implementation layers (engineering diagnostic), not the operator's conversational certification. |
| `WLJ_CERTIFICATION_PLATFORM_FUTURE.md` | Reference | Engineer | **DEFERRED — first type OPENED** | The unified WLJ Certification Platform. Its **first type is now built** (the Truth Validation Center realizes the reserved design: one evidence-capturing runner typed by `validation_type`, two-owner model, discovery-suite work-list, first-failing-layer). Remaining types (CRUD/Reasoning/Executive/Check-in/Domain) plug into the same engine — still deferred; do not build ahead of need. |
| `WLJ_MULTIMODAL_INTAKE_ARCHITECTURE.md` | Governing | Engineer | CURRENT | **Canonical architecture for ALL multimodal intake** — how a person hands the CoS images/video/audio/documents/structured files and it becomes deterministic, provenance-bearing, permanently retrievable truth. ONE platform every domain leverages (never a per-domain upload path). The model perceives; WLJ never interprets bytes; perceived content becomes a normal named intent tagged `source_artifact_id`+`confidence`; one arrival path → one artifact seam → one truth spine (validate→dedup→confirm→execute→audit→link) → first-class retrieval. Read FIRST for ANY upload/attachment/perception/artifact/OCR/transcription/document-intake work. Execution companion: `WLJ_MULTIMODAL_INTAKE_ROADMAP.md`. |
| `WLJ_MULTIMODAL_INTAKE_ROADMAP.md` | Reference | Engineer | CURRENT | Execution companion to the multimodal architecture — production-readiness scorecard (ranked by customer impact), phased plan (Phase 0 harden → Phase 1 ChatGPT-parity intake), preserve-don't-rebuild list, and the living milestone status ledger. Implementation progress lives here + changelog, never in the governing doc. |
| `WLJ_REQUEST_PATH_SAFETY.md` | Governing | Engineer | CURRENT | Never compute/LLM on the request path. |
| `WLJ_RUNTIME_TRACE_DEBUGGING.md` | Governing | Engineer | CURRENT | Prove the runtime path before editing. |
| `WLJ_JOURNAL_FORM_LAYOUT.md` | Governing | Engineer | CURRENT | **The Journal Create/Edit workspace layout + scroll model, and the reusable Workspace Dock (2026-07-21).** The app shell owns scrolling (one region, no viewport math); page content is plain document flow; the page owns layout while the shared RTE stays reusable. Presentation = the **Focus Compose** view: editor owns the screen + a sticky **Compose Dock** of always-visible domain metadata chips (open lightweight overlay pickers; live-update; zero layout shift) + in-dock Save. Records why six consecutive CSS patches failed (the interaction-model class) and the fixed visual hierarchy (mode → document → writing → metadata → save). First implementation of the **reusable WLJ Workspace Dock** pattern (`01 §6`); front-end invariants in `03 §6a`. Read before ANY Journal-form or workspace-dock work. |
| `WLJ_TIMESTAMP_PRECISION.md` | Governing | Engineer | CURRENT (Phase 1 shipped) | **A timestamp must never claim more precision than its source gave.** The reusable model `apps/core/truth/precision.py` (sibling of `temporal.py`): `Precision` vocabulary; `infer_precision`; `resolve_instant` (real time verbatim; date-only at noon **clamped ≤ now** — never future; precision reported); `format_instant` (honest per-precision render — DAY→"Today", never a clock time). Recommendation: first-class truth, adopted **per-domain, certification-gated**. Phase 1 (module + heart-rate/weight ingest dogfood) shipped 2026-07-20; Phase 2 (persist `observed_precision`) + Phase 3 (presentation) deferred with a full fabrication-site inventory. Read before ANY observed-timestamp ingest/display work. Principle folded into `03 §7`. |
| `LAYER1_DOMAIN_FRAMEWORK.md` + Legacy/Medication canon docs | Reference | Engineer | CURRENT | Certified domain patterns. |

## C. Milestone record & coverage (in `docs/`, CURRENT)

| Title | Authority | Audience | Purpose |
|---|---|---|---|
| `WLJ_MILESTONE_COS_ARCHITECTURE.md` | Reference | All | The final milestone report + recovery point. |
| `WLJ_VERSION_MANIFEST.md` | Reference | Operator | SHA/tag/versions/migrations. |
| `WLJ_ACCEPTANCE_BASELINE.md` | Governing | Engineer | Permanent regression suite (14 areas → tests). |
| `WLJ_DOCUMENTATION_INVENTORY.md` | Reference | All | 186-doc classification + the 14 uncertain docs. |
| `WLJ_OPERATIONS_VISION.md` | Governing | Engineer | **The WLJ Operations subsystem (architecture FROZEN 2026-07-11)** — Layer 1 truth domain; internal architecture + object model + package layout + import boundaries; recovery classification (R0–R4), lifecycle, maturity model (O0–O5); living roadmap + ledger. **Phase I visibility + Phase II framework + Phase II-B expanded R1 (three dark handlers / two shapes, incl. maturity-staleness coverage) SHIPPED (dark)**; Phase III deferred (evidence-gated); next = controlled production enablement. |
| `WLJ_OPERATIONS_PHASE2_PLAN.md` | Reference | Engineer | Deterministic Recovery engineering plan (IMPLEMENTED, ship-dark) + risk register; §11.1 production-enablement runbook; §14 handler comparison + evidence-backed Phase III deferral. |
| `WLJ_OPS_WALL_COVERAGE.md` | Operational | Operator | Observability coverage + OPS-1…10 backlog (Phase I as-built). |
| `WLJ_CONFIGURATION_GOVERNANCE.md` | Governing | Engineer | **OPS-13 foundation DEPLOYED report-only (prod-verified 2026-07-19); architecture RATIFIED 2026-07-19; OPS-14…17 approval-gated, not started.** WLJ owns deterministic configuration truth (the model never determines config validity; secrets never exposed/logged/persisted). Ratified decisions: blast-radius **milestone decomposition** (§10 — OPS-13 foundation → **OPS-14 Configuration Visibility (NEXT: a read-only Ops Wall `config_integrity` card, decoupled from global status)** → OPS-15 config→operations state + CoS banner → OPS-16 enforcement (high blast, report-only first) → OPS-17 continuous); explicit **service classes** runtime/build/administrative (§1A); **UNKNOWN** as a first-class aggregation state, never coerced to Healthy/Critical (§4A — the fail-to-unknown policy); **capability-first vision** (Configuration Governance is one deterministic contributor to Platform Capability Verification, composing existing authorities, never a new one, §11); self-reporting **manifests are the primary authority** (Railway API only ever optional secondary). Read FIRST for any config-drift / config-visibility / OPS-14+ work. |
| `WLJ_OPERATIONS_TRUTH_PATH_INVESTIGATION.md` · `WLJ_OPERATIONS_INCIDENT_TIMELINE_2026-07-23.md` | Reference (investigation) | Engineer | **Evidence trail for the 2026-07-23 Operations truth divergence (CLOSED — corrected & fixed by `f871a511`).** The truth-path doc traced the full Operations flow and *hypothesised* two disagreeing authorities (COAS vs Integrity); the timeline doc **falsified** the messaging half from prod rows (0 `operations_alert` notifications) and pinned the real cause to a *single* integrity authority flapping across the score boundary + the always-green alignment badge. Durable lessons folded to `03 §4a` (prove/falsify an architectural hypothesis) + `03 §7a` (honest status under noise). The latent two-authority *risk* is documented but was NOT this incident's cause; consolidation deferred until evidence warrants. |
| `WLJ_CURRENT_CONTEXT_HELP_COVERAGE.md` | Reference | Engineer | Page-coverage audit + CC/HELP backlog. |
| `WLJ_RELEASE_POLICY.md` | Governing | All | Three publication levels. |
| `WLJ_PRODUCTION_RUNBOOKS.md` | Operational | Operator | Failures, rollback, recovery, streaming, OpenAI, workers, Redis, Postgres. |
| `WLJ_SECURITY_AUTHORIZATION_FRAMEWORK.md` | Governing | Engineer | **RATIFIED 2026-07-18 (architecture only — not implemented).** The complete long-term *authorization* model: primitives **Identity · Space · Capability · Ownership · Delegation · Trust**; one deterministic PDP; roles = capability bundles; **Space is the canonical container** (every resource belongs to exactly one; ownership/capabilities/authorization evaluated within a Space; the Platform is NOT a Space); AI is a derived principal that never self-authorizes; platform authority is separate from data ownership. Fits within the Constitution (no Review; Article VI NOT created). Distinct from `WLJ_SECURITY_PRIVACY_RETENTION.md` (that = privacy/retention/audit-of-data; this = who-may-do-what). Read before ANY authorization/identity/admin/sharing/AI-authority work. |
| `WLJ_SECURITY_PRIVACY_RETENTION.md` | Governing | Engineer | Ownership, 72h image retention (locked), audit, provenance. |
| `WLJ_KNOWN_LIMITATIONS.md` | Reference | All | Honest, phased limitations. |

## D. Project operating doc

| Title | Authority | Audience | Status | Purpose |
|---|---|---|---|---|
| `CLAUDE.md` (repo root) | Governing | Engineer | CURRENT | The in-repo operating instructions Claude Code loads each session. Should reference this package; keep in sync when governing docs move. |
| `docs/wlj_claude_changelog.md` | Operational | Engineer | CURRENT | Level-1 technical changelog (every commit). |
| `docs/wlj_claude_troubleshoot.md` | Reference | Engineer | CURRENT | Known-issue patterns (check first). |

## E. Historical & archived (preserved, not current)

Do **not** treat these as current design. The retired reasoning-engine ("Conductor / lanes / classifier / in-process Beth") docs are bannered HISTORICAL and must not be restored without a Constitutional Review.

- Full classification (95 CURRENT / 39 HISTORICAL / 3 SUPERSEDED / 49 ARCHIVE): `docs/WLJ_DOCUMENTATION_INVENTORY.md`.
- Retired startup material (superseded by this package): `@WLJ_SYSTEM_PROMPTS/_ARCHIVE_SUPERSEDED_STARTUP/*` (the former `00_CORE_STARTUP` docs) and the earlier mode/reference folders `01_`–`08_` (supporting/legacy; consult only for history).
- **14 boundary docs pending Danny's judgment** before final classification — see inventory §6.

---

*Keep this index current: when a governing doc is added, moved, or reclassified, update the matching row here in the same change.*
