"""
apps.core.truth — platform Canonical Truth capabilities.

Domain-agnostic building blocks every domain consumes (Health first, then Finance,
Faith, Calendar, Relationships, …). These are PLATFORM capabilities, not domain
features: implement once here, consume everywhere. See docs/BETH_LAYER1_TRUTH_INVENTORY.md.

Capabilities:
- freshness — current / stale / pending / partial / missing verdicts (Law 1).
"""
